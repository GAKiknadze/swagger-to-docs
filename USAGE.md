# 📖 Подробное руководство использования

## Содержание

1. [Установка](#установка)
2. [Быстрый старт](#быстрый-старт)
3. [Использование CLI](#использование-cli)
4. [Программное использование](#программное-использование)
5. [Примеры](#примеры)
6. [Советы и трюки](#советы-и-трюки)

## Установка

### Требования

- Python 3.10+
- pip

### Основная установка

```bash
git clone <repository>
cd swagger-to-docs
pip install -e .
```

### С поддержкой Ollama (локально)

```bash
pip install -e ".[ollama]"
```

### С поддержкой OpenAI

```bash
pip install -e ".[openai]"
```

### С поддержкой Claude (Anthropic)

```bash
pip install -e ".[anthropic]"
```

### Все провайдеры сразу

```bash
pip install -e ".[ollama,openai,anthropic]"
```

## Быстрый старт

### Вариант 1: Используя интерактивный CLI (самый простой)

```bash
python cli.py
```

Следуйте интерактивному меню для:
- Выбора LLM провайдера
- Конфигурации
- Обработки файлов
- Анализа спецификаций

### Вариант 2: Используя готовые примеры

```bash
# С Ollama (убедитесь, что Ollama запущена)
python examples.py 1

# С OpenAI
OPENAI_API_KEY=sk-... python examples.py 2

# С Claude
ANTHROPIC_API_KEY=sk-ant-... python examples.py 3
```

### Вариант 3: Используя основной скрипт

```bash
python main.py
```

## Использование CLI

### Основной интерфейс

```bash
python cli.py
```

### Меню опций

```
Главное меню:
  1. Обработать OpenAPI файл/директорию
  2. Анализировать OpenAPI спецификацию
  3. Экспортировать в разные форматы
  4. Валидировать OpenAPI файл
  5. Настроить конфигурацию
  6. Выход
```

### Пример использования каждой опции

#### Опция 1: Обработка файлов

```
Введите путь к файлу или директории (examples): examples
```

Генератор создаст:
- Директорию `docs/<api_name>/`
- Поддиректории `tags/` и `endpoints/`
- Markdown файлы для каждого тега и эндпоинта
- `README.md` с оглавлением

#### Опция 2: Анализ спецификации

```
Введите путь к OpenAPI файлу: examples/petstore.json
```

Выведет:
- Статистику (количество эндпоинтов, тегов, схем)
- Методы HTTP
- Информацию о безопасности
- Опциональный список всех эндпоинтов

#### Опция 3: Экспорт данных

```
Введите путь к OpenAPI файлу: examples/petstore.json
```

Выберите формат:
- **CSV**: список эндпоинтов с методами, путями, описаниями
- **JSON**: статистика и метаинформация
- **Postman**: коллекция для импорта в Postman

#### Опция 4: Валидация

```
Введите путь к OpenAPI файлу: examples/petstore.json
```

Проверит валидность спецификации и выведет:
- ✅ Валидна / ❌ Ошибки валидации
- Детали (название, версия)
- Список ошибок (если есть)

#### Опция 5: Конфигурация

Интерактивная конфигурация:
1. Выбор LLM провайдера (Ollama, OpenAI, Anthropic, HuggingFace)
2. Выбор модели
3. API ключ (если требуется)
4. Язык документации (ru/en)
5. Директория для выходных файлов

## Программное использование

### Пример 1: Базовое использование

```python
from config import LLMConfig, get_llm_from_config
from advanced_generator import AdvancedOpenAPIDocGenerator

# Инициализируем LLM
llm_config = LLMConfig(provider="ollama", model="mistral")
llm = get_llm_from_config(llm_config)

# Создаем генератор
generator = AdvancedOpenAPIDocGenerator(llm, output_dir="docs")

# Обрабатываем директорию
generator.process_directory("examples")
```

### Пример 2: Анализ спецификации

```python
from utils import load_openapi, OpenAPIAnalyzer

# Загружаем спецификацию
spec = load_openapi("examples/petstore.json")

# Создаем анализатор
analyzer = OpenAPIAnalyzer(spec)

# Получаем статистику
stats = analyzer.get_statistics()
print(f"Всего эндпоинтов: {stats['total_endpoints']}")
print(f"Теги: {stats['tags']}")

# Находим эндпоинты по тегу
pets_endpoints = analyzer.find_endpoints_by_tag("pets")
for ep in pets_endpoints:
    print(f"{ep['method']} {ep['path']}")

# Получаем список всех эндпоинтов
all_endpoints = analyzer.list_all_endpoints()
for ep in all_endpoints:
    print(f"{ep['method']:6} {ep['path']}")
```

### Пример 3: Экспорт в разные форматы

```python
from utils import load_openapi, OpenAPIExporter

spec = load_openapi("examples/petstore.json")
exporter = OpenAPIExporter(spec)

# CSV экспорт
exporter.export_endpoints_csv("endpoints.csv")

# JSON статистика
exporter.export_statistics_json("stats.json")

# Postman коллекция
exporter.generate_postman_collection("postman.json")
```

### Пример 4: Валидация

```python
from utils import load_openapi, OpenAPIValidator

spec = load_openapi("examples/petstore.json")
is_valid, errors = OpenAPIValidator.is_valid_openapi(spec)

if is_valid:
    print("✅ Спецификация валидна!")
else:
    print("❌ Ошибки:")
    for error in errors:
        print(f"  - {error}")
```

### Пример 5: Получение информации об эндпоинте

```python
from utils import load_openapi, OpenAPIAnalyzer

spec = load_openapi("examples/petstore.json")
analyzer = OpenAPIAnalyzer(spec)

# Получить схему request body
request_schema = analyzer.get_request_body_schema("/pets", "post")
print(request_schema)

# Получить схемы ответов
responses = analyzer.get_response_schemas("/pets", "get")
for status, schema in responses.items():
    print(f"Status {status}: {schema}")

# Найти все эндпоинты с методом POST
post_endpoints = analyzer.find_endpoints_by_method("post")
for ep in post_endpoints:
    print(f"POST {ep['path']}")
```

### Пример 6: Работа с разными LLM провайдерами

```python
from config import LLMConfig, get_llm_from_config
from advanced_generator import AdvancedOpenAPIDocGenerator

# OpenAI
llm_openai = get_llm_from_config(
    LLMConfig(
        provider="openai",
        model="gpt-3.5-turbo",
        api_key="sk-..."
    )
)

# Claude
llm_claude = get_llm_from_config(
    LLMConfig(
        provider="anthropic",
        model="claude-3-sonnet-20240229",
        api_key="sk-ant-..."
    )
)

# Ollama (локально)
llm_ollama = get_llm_from_config(
    LLMConfig(
        provider="ollama",
        model="mistral"
    )
)

# Используем с генератором
generator = AdvancedOpenAPIDocGenerator(llm_openai)
generator.process_directory("examples")
```

## Примеры

### Пример 1: Генерация документации для Petstore API

```bash
python cli.py
# Выбрать опцию 1
# Ввести: examples/petstore.json
```

Результат:
```
docs/
├── swagger_petstore/
│   ├── README.md              # Оглавление
│   ├── tags/
│   │   ├── pets.md            # Документация группы pets
│   │   └── ...
│   └── endpoints/
│       ├── get_pets.md        # GET /pets
│       ├── post_pets.md       # POST /pets
│       ├── get_pets_petid.md  # GET /pets/{petId}
│       └── ...
```

### Пример 2: Экспорт статистики

```bash
python cli.py
# Выбрать опцию 3
# Выбрать опцию 2 (JSON статистика)
```

Результат (`exports/petstore.json`):
```json
{
  "title": "Swagger Petstore",
  "version": "1.0.0",
  "total_endpoints": 3,
  "methods": {
    "get": 2,
    "post": 1
  },
  "tags": {
    "pets": 3
  },
  "schemas": 3,
  "security_schemes": 0
}
```

### Пример 3: Анализ API

```bash
python cli.py
# Выбрать опцию 2
# Ввести путь к файлу
# Ответить yes на вопрос о показе эндпоинтов
```

Вывод:
```
✅ OpenAPI спецификация валидна

📈 Статистика:
   Название: Swagger Petstore
   Версия: 1.0.0
   Всего эндпоинтов: 3
   Методы: {'get': 2, 'post': 1}
   Теги: 1
   Схемы: 3
   Схемы безопасности: 0

🏷️  Теги и их эндпоинты:
   - pets: 3 эндпоинтов

🔐 Схемы безопасности:
   Нет определенных схем

GET    /pets                          [pets]
POST   /pets                          [pets]
GET    /pets/{petId}                  [pets]
```

## Советы и трюки

### Советы по использованию

1. **Используйте интерактивный CLI для начинающих**
   - `python cli.py` имеет интерактивный интерфейс
   - Все опции объяснены в меню

2. **Сохраняйте конфигурацию в .env**
   ```bash
   cp .env.example .env
   # Отредактируйте .env с вашей конфигурацией
   ```

3. **Используйте Ollama для локальной разработки**
   - Не требует API ключей
   - Работает оффлайн
   - Модели загружаются один раз

4. **Проверяйте валидность перед обработкой**
   ```bash
   python cli.py
   # Опция 4: Валидировать файл
   ```

5. **Экспортируйте для использования в других инструментах**
   - CSV для Excel/Google Sheets
   - Postman JSON для импорта в Postman
   - JSON для программной обработки

### Ускорение обработки

1. **Обрабатывайте отдельные файлы вместо директорий**
   ```bash
   python cli.py
   # Опция 1, вместо all → examples/petstore.json
   ```

2. **Используйте более быстрые модели**
   ```python
   LLMConfig(model="mistral")  # Быстрее чем GPT-4
   ```

3. **Увеличивайте max_tokens только если нужно**
   ```python
   LLMConfig(max_tokens=1500)  # Вместо 2000
   ```

### Проблемы и решения

#### Ollama не отвечает

```bash
# Проверьте, запущена ли Ollama
ollama serve

# Проверьте доступные модели
ollama list

# Скачайте нужную модель
ollama pull mistral
```

#### OpenAI API ошибка

```bash
# Проверьте API ключ
export OPENAI_API_KEY="sk-..."

# Убедитесь в правильности ключа
python -c "from langchain_openai import ChatOpenAI; ChatOpenAI(api_key='$OPENAI_API_KEY', model_name='gpt-3.5-turbo')"
```

#### Ошибка парсинга JSON

```python
import json

with open("file.json") as f:
    try:
        json.load(f)
        print("✅ JSON валидный")
    except json.JSONDecodeError as e:
        print(f"❌ Ошибка парсинга: {e}")
```

#### Недостаточно памяти

```bash
# Обрабатывайте файлы отдельно вместо директории
# Или используйте более легкую модель
python cli.py  # Выбрать меньшую модель
```

### Оптимизация памяти

1. **Используйте quantized модели**
   ```bash
   ollama pull mistral:latest
   ```

2. **Уменьшайте max_tokens**
   ```python
   LLMConfig(max_tokens=1000)
   ```

3. **Обрабатывайте небольшие файлы**
   ```bash
   python cli.py
   # Опция 1 → examples/petstore.json (вместо всей директории)
   ```

---

**Для дополнительной помощи:** см. [README.md](README.md)
